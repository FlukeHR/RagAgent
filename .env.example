# Optional override. When omitted, a random key is created in data/secrets/master.key.
PAPER_RAG_MASTER_KEY=

# Legacy server-wide model fallback. Normal dashboard use stores per-user profiles.
OPENAI_API_KEY=
